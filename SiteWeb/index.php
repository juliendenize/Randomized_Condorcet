<?php
  session_start()

  include('controller.php')

  if(isset($_GET['action']) ) {
    $vue = $_GET['action'];
    if( {

    }
    else include('bienvenue.php');
  }
  else include('bienvenue.php');
